# Pinned versions for the FLUX.2 Klein worker on RunPod 5090 / Blackwell sm_120.
# Base image: runpod/pytorch:1.0.3-cu1281-torch291-ubuntu2404
# Note: installing diffusers from main upgrades torch to 2.11.0+cu130 (auto)
# Use `pip install --break-system-packages -r requirements.txt`. Do NOT install bitsandbytes.

numpy
pillow
safetensors
sentencepiece
protobuf
accelerate
huggingface_hub
transformers>=5.5.0
# Pinned to the exact commit installed on the production Klein pod. The pod
# was running diffusers 0.38.0.dev0 from this SHA, verified via
# dist-info/direct_url.json. Unpinning (= tracking main) reintroduces a
# reproducibility hole: diffusers main moves faster than torch/accelerate
# can stay compatible, so a fresh install can pull in a breaking change.
git+https://github.com/huggingface/diffusers.git@160852de680d36117e0a787f7f8b718232539abb
torchvision
